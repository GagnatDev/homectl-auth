// @gagnatdev/homectl-auth-client/browser
// Browser-side token helper — full implementation in Phase 6.

export type AuthBrowserClientOptions = {
  /** Base URL of the auth service, e.g. https://auth.homectl.no */
  authServiceUrl: string;
};

// Stub — full implementation in Phase 6
export function createAuthBrowserClient(_options: AuthBrowserClientOptions): never {
  throw new Error('@gagnatdev/homectl-auth-client/browser — not implemented yet (Phase 6)');
}
