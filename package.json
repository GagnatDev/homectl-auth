{
  "name": "homectl-auth",
  "private": true,
  "scripts": {
    "dev": "docker compose -f docker-compose.dev.yml up --build",
    "dev:clean": "docker compose -f docker-compose.dev.yml down -v --rmi local",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "build": "pnpm -r build"
  }
}
